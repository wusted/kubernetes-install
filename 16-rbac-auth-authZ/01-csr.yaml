apiVersion: certificates.k8s.io/v1
kind: CertificateSigningRequest
metadata:
  name: developer-user
spec: # Replace 'request' wit csr file content in base64 # cat developer-user.csr | base64 | tr -d "\n"
  request: REPLACE_WITH_BASE64
  signerName: kubernetes.io/kube-apiserver-client
  expirationSeconds: 86400  # one day
  usages:
  - client auth